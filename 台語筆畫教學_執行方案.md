# 台語詞彙筆畫教學　平板應用執行方案

專案名稱：看無字，講有聲 — 台語詞彙筆畫教學互動應用  
適用裝置：平板（iPad / Android Tablet）  
字形來源：客製切角黑體標準字（設計師自製 AI 向量檔）

---

## 一、技術選型結論

### 核心動畫方式：SVG stroke-dashoffset

每個字的每一筆畫是一條獨立的 SVG `<path>`，初始時用 `stroke-dashoffset` 把筆畫「推離可視範圍」，動畫播放時將 offset 歸零，筆畫逐漸「被畫出來」。這是目前所有漢字筆畫動畫的業界標準做法。

放棄矩形遮色片方案的原因：雖然粗黑體容錯高，但這批字含有大量走之底（迌、遊、浮）、三點水（消、浮）、心底（感心）等曲線筆畫，矩形無法貼合曲線，會在彎折處出現視覺破綻。stroke-dashoffset 可以完整處理任意形狀的筆畫。

### 渲染平台：SVG + HTML5 Canvas（互動練習層）

- 動畫播放：純 SVG + CSS/JS 控制
- 互動練習（選配）：HTML5 Canvas 接收觸控輸入，對比筆畫中線座標判斷正確性

### 開發框架建議

平板 Web App：React + Vite，部署為 PWA（可離線使用）  
原生 App（若有預算）：React Native，使用 SVG 套件 react-native-svg

---

## 二、畫面分層架構

每個字的教學畫面由三層疊合組成，從下到上：

```
┌─────────────────────────────────────┐
│  第三層（最上）：設計稿 PNG             │  ← 透明背景，純裝飾
│  字帖版型、色塊、拼音、圖示等視覺元素     │     不參與動畫，pointer-events: none
├─────────────────────────────────────┤
│  第二層（中）：SVG 動畫層              │  ← 筆畫 path，依序顯現
│  每一筆畫是獨立 <path>               │     stroke-dashoffset 控制出現時機
│  初始全部隱藏，播放時逐畫 fade in       │
├─────────────────────────────────────┤
│  第一層（最下）：格線底稿              │  ← 靜態，opacity 10–15%
│  字形輪廓描摹參考線                   │     讓使用者知道字寫在哪裡
└─────────────────────────────────────┘
```

### 設計端對應的資產輸出

| 圖層 | 輸出格式 | 說明 |
|------|---------|------|
| 設計稿裝飾層 | PNG（透明背景）| 從 .ai 原稿隱藏筆畫圖層後匯出 |
| 筆畫動畫層 | SVG | 獨立工作檔匯出，每畫一個 `<path>` |
| 格線底稿 | PNG 或 SVG | 字形輪廓線稿，單色，匯出後在 CSS 設定低透明度 |

### 為什麼設計稿用 PNG 而非 SVG

設計稿含有大量裝飾元素（色塊、排版文字、圖示），如果也用 SVG 疊加，會與筆畫 SVG 的 `id` 命名空間衝突，也會讓 DOM 結構複雜化。PNG 在平板解析度下顯示品質足夠，且疊加方式最單純。

---

## 三、Illustrator 工作流程建議

### 檔案分離原則

不在設計稿原始檔裡做拆筆畫，另開獨立工作檔：

```
給展哥.ai              → 設計稿原始檔（不動）
strokes/
  迌_strokes.ai       → 「迌」的拆筆畫工作檔
  迌_strokes.svg      → 從工作檔匯出的最終 SVG
assets/
  迌_design.png       → 設計稿透明背景 PNG（隱藏筆畫層後匯出）
  迌_guide.png        → 格線底稿 PNG
```

### 拆筆畫步驟

1. 從設計稿複製單字 path，貼入新的工作檔
2. 若為複合路徑，執行「物件 → 複合路徑 → 釋放」拆開
3. 將每一畫分成獨立 path 物件
4. 在圖層面板依筆順命名：`stroke-01`、`stroke-02`…
   （命名會直接變成匯出 SVG 的 `id`，開發端可直接使用）
5. 匯出 SVG 時選「保留圖層名稱為 ID」

### 注意事項

- 複合路徑要先 Release 再拆，否則整個字會是同一個 `<path>` 無法個別控制
- 筆畫命名從 `stroke-01` 開始（兩位數），避免排序錯誤（`stroke-9` 會排在 `stroke-10` 之後）
- 每個字完成後先在瀏覽器確認 SVG `id` 正確，再繼續做下一個字

---

## 四、字形資產處理流程

這是整個專案最核心、最費工的部分，必須從設計師的 AI 原始檔出發。

### 步驟 1：取得向量原始檔

向設計師要求提供每個詞彙、每個字的 Illustrator（.ai）或 SVG 檔案。  
確認字形是「已展開的輪廓路徑」（Outline），而非字型文字物件。

### 步驟 2：在 Illustrator 中拆筆畫

每個字需要手動拆解，將每一畫拆成獨立的 path 物件，並依筆順命名：

```
stroke-01, stroke-02, stroke-03 ... stroke-N
```

這批字為粗切角黑體，筆畫間有明顯空隙，拆解相對容易。  
每個字預計花費 15–30 分鐘（依筆畫複雜度）。

**字數估算（依圖片所見）：**

| 詞彙 | 台羅拼音 | 字數 | 估計難度 |
|------|---------|------|---------|
| 迌迌 | tshit-thô | 2 | 高（走之底） |
| 幼秀 | iù-siù | 2 | 中 |
| 獅仔 | sut-á | 2 | 中 |
| 感心 | tsheh-sim | 2 | 高（心底） |
| 鉸鉸 | mê-kak | 2 | 中 |
| 偏仙仔 | piàn-sian-á | 3 | 高 |
| 幼秀 | iù-siù | 2 | 中 |
| 浮浪貢 | phû-lông-kòng | 3 | 高（三點水） |
| 花貓 | hue-pa-ji-niau | 2 | 中 |
| 粗粗 | tshia-... | 2 | 低 |

預估總字數：約 25–35 個字（含詞彙內重複字）

### 步驟 3：定義筆畫中線座標（median）

僅在需要「互動練習模式」時才需要這步。  
每條筆畫的中線是一組座標點列表，用來判斷使用者的觸控軌跡方向是否正確。

如果只做「觀看示範動畫」，可跳過此步驟，大幅縮短製作時間。

### 步驟 4：匯出並建立資料結構

每個字匯出為一個 JSON 檔案：

```json
{
  "character": "迌",
  "romanization": "tshit-thô",
  "totalStrokes": 9,
  "strokes": [
    {
      "id": "stroke-01",
      "name": "點",
      "path": "M 100 25 C 98 20 105 18 108 22 Z",
      "median": [[100,25],[104,22],[108,20]]
    },
    {
      "id": "stroke-02",
      "name": "橫折",
      "path": "M 55 38 L 110 38 L 110 78",
      "median": [[55,38],[110,38],[110,78]]
    }
  ]
}
```

---

## 五、平板應用功能規格

### 畫面一：詞彙選單

- 卡片式排列，每張卡片顯示詞彙大字 + 台羅拼音
- 點擊進入該詞彙的筆畫教學頁

### 畫面二：筆畫教學主畫面

畫面中央的字格由三層疊合呈現（對應第二章的分層架構）：底層是格線底稿（10% 透明度）、中層是 SVG 筆畫動畫、最上層是設計稿裝飾 PNG。

**示範播放模式**

- 中央大字格，底稿輪廓以低透明度顯示，讓使用者看見字的位置
- 播放鍵啟動後，SVG 中間層的筆畫依序以 stroke-dashoffset 動畫顯現
- 每畫出現時，左側顯示對應筆畫名稱（橫、豎、撇、捺…）
- 速度控制：慢 / 中 / 快（對應 animation-duration 調整）
- 「逐畫前進」模式：手動點擊下一畫，適合教學情境

**互動練習模式（選配，第二階段開發）**

- Canvas 層疊在 SVG 底稿上方，接收觸控輸入
- 判斷邏輯：筆畫起點落點、移動方向、終點與 median 比對
- 寬容度可調，建議初始設定較寬（適合非母語學習者）
- 答對：該筆畫 fade in 顯現，提示音效
- 答錯三次：自動播放該筆畫示範動畫作為提示

### 畫面三：詞彙完成頁

- 整個詞彙的所有字完成後，顯示完整詞彙 + 台羅拼音 + 詞義說明
- 可加入簡短例句或文化說明

---

## 六、開發工作量估算

### 設計端（設計師負責）

| 工作項目 | 估計時間 |
|---------|---------|
| 拆筆畫（每字平均 20 分鐘，約 30 字） | 10 小時 |
| 定義筆順順序、命名（stroke-01 起） | 2 小時 |
| 匯出設計稿透明 PNG（每張字帖） | 2 小時 |
| 匯出格線底稿 PNG（每個字） | 1 小時 |
| 匯出筆畫 SVG、整理檔案結構 | 2 小時 |
| **小計** | **約 17 小時** |

### 開發端（前端工程師負責）

| 工作項目 | 估計時間 |
|---------|---------|
| 建立 React + Vite 專案架構 | 4 小時 |
| SVG 動畫播放引擎（通用元件） | 8 小時 |
| 詞彙選單頁面 | 4 小時 |
| 教學主畫面（示範播放） | 8 小時 |
| 互動練習模式（Canvas + 手寫辨識） | 16 小時 |
| 整合所有字形資料 | 4 小時 |
| 平板 UI 調適 + PWA 設定 | 4 小時 |
| **小計（含練習模式）** | **約 48 小時** |
| **小計（僅示範播放）** | **約 28 小時** |

---

## 七、建議開發順序

```
Phase 1：驗證核心技術（1 週）
  → 選 2 個字（一個直線結構、一個含彎筆）
  → 完整走完拆筆畫 → JSON → SVG 動畫的流程
  → 確認在平板上的顯示效果與觸控反應

Phase 2：示範播放完整版（2–3 週）
  → 補完所有字的 SVG 資料
  → 完成選單、教學頁、完成頁
  → 整合台羅拼音、詞義文案

Phase 3：互動練習模式（2–3 週，選配）
  → 補齊所有字的 median 座標
  → 實作 Canvas 觸控辨識邏輯
  → 測試寬容度調整
```

---

## 八、已知風險與對應策略

| 風險 | 說明 | 對應方式 |
|------|------|---------|
| 向量檔拿不到 | 設計師可能只有 PDF 或點陣圖 | 需從頭描繪，工時 × 3；要求設計師提供 .ai 原始檔 |
| 自製字不在 Unicode | 某些台語用字可能是造字 | 全程用 SVG path 處理，不依賴字型編碼 |
| 筆畫切角裝飾影響可讀性 | 動畫播放時切角字的「筆畫感」可能不直覺 | Phase 1 驗證時一起確認，必要時在動畫版使用簡化筆畫輪廓 |
| 平板效能 | 複雜 SVG 在低階平板可能有掉幀 | 測試時加入 Canvas renderer 選項作為備援 |
